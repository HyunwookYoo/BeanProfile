import 'package:flutter/material.dart';

class BeanFormScreen extends StatelessWidget {
  const BeanFormScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('원두 추가')));
}
